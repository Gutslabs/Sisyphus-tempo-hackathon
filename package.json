{
  "name": "@tempo-agent/app",
  "version": "0.1.0",
  "private": true,
  "description": "Sisyphus web app (AI chat + Tempo on-chain operations)",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "lint": "npm --prefix web run lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
