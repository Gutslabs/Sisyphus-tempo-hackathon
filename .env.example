# ===== Sisyphus Web (AI Chat + Tempo On-chain Ops) =====

# Tempo RPC (optional; defaults to Moderato)
TEMPO_RPC_URL=https://rpc.moderato.tempo.xyz

# Keeper wallet for on-chain scheduled/recurring payments execution (execute-due route + cron)
# NEVER commit real keys.
KEEPER_PRIVATE_KEY=0x_YOUR_PRIVATE_KEY_HERE

# Optional: protect /api/tempo/schedules/execute-due with a bearer token
CRON_SECRET=

# AI (xAI Grok)
GROK_API_KEY=

# Privy (required for wallet login UI)
NEXT_PUBLIC_PRIVY_APP_ID=

# Optional: Supabase (tx history + chat sessions)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ===== Routing: landing vs dashboard (used by middleware + deploy) =====
# App URL = dashboard. Everything else (root domain, www, IP) = landing.
# Optional: if you use a separate app subdomain (e.g. app.example.com), set this so middleware knows which host shows the dashboard.
# Leave empty to disable host-based landing/dashboard routing (dashboard will be available at / on all hosts).
NEXT_PUBLIC_APP_URL=

# ===== Deploy (EC2): ./scripts/deploy-ec2.sh loads .env + web/.env.local + contracts/.env =====
# Domain for nginx server_name + SSL (certbot). Script can derive from NEXT_PUBLIC_APP_URL if unset.
DEPLOY_DOMAIN=
DEPLOY_SSL_EMAIL=
